// External destinations, in one place so they're easy to repoint.
//
// LAUNCHER_URL is where "Install the launcher" sends people. For now it's
// the source repo (build from source); when there are tagged builds this
// should become the releases page.
export const GITHUB_REPO_URL = "https://github.com/Reilynn-Olsen/ptcgl-tracker";
export const LAUNCHER_URL = GITHUB_REPO_URL;
